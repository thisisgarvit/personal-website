# Elevation implementation plan

**Contract:** `ELEVATION-BRIEF.md`, Addendum 2, and
`research/pm-website-patterns.md` extend the locked design. The mascot is a
session journey tracker; on-call naming is retired.

1. Own one schema-validated, capped `sessionStorage` event stream for the
   current tab. Feed both the mascot milestone bus and visible funnel from it;
   transmit nothing.
2. Render the five live Amplitude-style stages with step-over-step status and
   an explicitly authored muted typical-visitor overlay. Add only the four
   small readouts and one or two candid insights specified by the brief.
3. Replace the procedural figure with adapted Quaternius CC0 rigged assets,
   authored garment/hair, palette reskin, headset/pager, look-at, wave, guide,
   milestone, board, and conversion reactions. Preserve poster-first fallback,
   capability gates, visibility pause, and failure boundaries.
4. Extend the design contract with functional material/blur hierarchy,
   separately authored dark values, surface lighting, elevation, and physical
   control feedback. Keep product mechanics and content unchanged.
5. Upgrade 404 to the requested tiny blameless SEV-3 postmortem. Do not build
   any other researched post-launch pattern.
6. Verify unit, type, lint, build, budget, same-origin privacy, journey/404 E2E,
   accessibility, and rendered 1440×900 / 390×844 light+dark states before
   integration.
